-- ================================================================
-- UNIVERSAL HUB - RAYFIELD UI (Chính thống từ sirius.menu/rayfield)
-- GỘP TOÀN BỘ: Player, Combat, Aim (smoothness, tia đỏ, hitbox),
-- Teleport, ESP, Misc. F5 mở/đóng menu.
-- ================================================================

-- ===== LOAD RAYFIELD UI =====
local Rayfield = nil
local success, err = pcall(function()
    Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
end)
if not success or not Rayfield then
    pcall(function()
        Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/UI-Interface/CustomFIeld/main/RayField.lua'))()
    end)
end
if not Rayfield then
    warn("Không thể load Rayfield UI. Kiểm tra kết nối mạng.")
    return
end

-- ===== BIẾN TOÀN CỤC =====
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")

-- ===== LẤY CHARACTER =====
local function GetCharacter()
    return LocalPlayer.Character
end
local function GetHumanoid()
    local c = GetCharacter()
    return c and c:FindFirstChild("Humanoid")
end
local function GetRootPart()
    local c = GetCharacter()
    return c and c:FindFirstChild("HumanoidRootPart")
end

-- ===== TẠO WINDOW =====
local Window = Rayfield:CreateWindow({
    Name = "UNIVERSAL HUB",
    LoadingTitle = "Đang tải...",
    LoadingSubtitle = "by nhatanh Dev",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "UniversalHub",
        FileName = "Settings"
    },
    Discord = { Enabled = false, Invite = "", RememberJoins = false },
    KeySystem = false,
    KeySettings = {
        Title = "Key System",
        Subtitle = "Nhập key",
        Note = "Liên hệ admin",
        FileName = "Key",
        SaveKey = true,
        GrabKeyFromSite = false,
        Key = {"free"}
    }
})

-- ===== TẠO TAB =====
local PlayerTab = Window:CreateTab("👤 Người chơi", 4483362458)
local CombatTab = Window:CreateTab("⚔️ Chiến đấu", 4483362458)
local AimTab = Window:CreateTab("🎯 Aim", 4483362458)
local TeleportTab = Window:CreateTab("🚀 Dịch chuyển", 4483362458)
local ESPTab = Window:CreateTab("👁️ ESP", 4483362458)
local MiscTab = Window:CreateTab("⚙️ Tùy chỉnh", 4483362458)

-- ================================================================
-- TAB NGƯỜI CHƠI
-- ================================================================
local PlayerSection = PlayerTab:CreateSection("Điều khiển nhân vật")

PlayerTab:CreateSlider({
    Name = "Tốc độ di chuyển",
    Range = {16, 250},
    Increment = 1,
    Suffix = "Speed",
    CurrentValue = 16,
    Flag = "WalkSpeed",
    Callback = function(Value)
        local hum = GetHumanoid()
        if hum then hum.WalkSpeed = Value end
    end
})

PlayerTab:CreateSlider({
    Name = "Sức bật",
    Range = {50, 500},
    Increment = 5,
    Suffix = "Jump",
    CurrentValue = 50,
    Flag = "JumpPower",
    Callback = function(Value)
        local hum = GetHumanoid()
        if hum then hum.JumpPower = Value end
    end
})

-- Fly
local flyEnabled = false
local flyBodyVelocity = nil
local flyConnection = nil
PlayerTab:CreateToggle({
    Name = "Bay (WASD + Space/Shift)",
    CurrentValue = false,
    Flag = "Fly",
    Callback = function(State)
        flyEnabled = State
        if State then
            local root = GetRootPart()
            if not root then return end
            flyBodyVelocity = Instance.new("BodyVelocity")
            flyBodyVelocity.Velocity = Vector3.new(0,0,0)
            flyBodyVelocity.MaxForce = Vector3.new(4000,4000,4000)
            flyBodyVelocity.Parent = root
            if flyConnection then flyConnection:Disconnect() end
            flyConnection = RunService.Heartbeat:Connect(function()
                if not flyEnabled or not flyBodyVelocity or not flyBodyVelocity.Parent then
                    if flyBodyVelocity then flyBodyVelocity:Destroy() end
                    return
                end
                local move = Vector3.new(0,0,0)
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then move = move + Vector3.new(0,0,-1) end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then move = move + Vector3.new(0,0,1) end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then move = move + Vector3.new(-1,0,0) end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then move = move + Vector3.new(1,0,0) end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space) then move = move + Vector3.new(0,1,0) end
                if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then move = move + Vector3.new(0,-1,0) end
                if move.Magnitude > 0 then
                    flyBodyVelocity.Velocity = move.Unit * 60
                else
                    flyBodyVelocity.Velocity = Vector3.new(0,0,0)
                end
            end)
        else
            if flyBodyVelocity then flyBodyVelocity:Destroy() end
            if flyConnection then flyConnection:Disconnect() end
        end
    end
})

-- Noclip
local noclipEnabled = false
PlayerTab:CreateToggle({
    Name = "Noclip (xuyên tường)",
    CurrentValue = false,
    Flag = "Noclip",
    Callback = function(State)
        noclipEnabled = State
    end
})
RunService.Stepped:Connect(function()
    if noclipEnabled then
        local c = GetCharacter()
        if c then
            for _, part in ipairs(c:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end
end)

-- ================================================================
-- TAB CHIẾN ĐẤU
-- ================================================================
local CombatSection = CombatTab:CreateSection("Tự động chiến đấu")

-- Auto Click
local autoClickEnabled = false
local autoClickThread = nil
CombatTab:CreateToggle({
    Name = "Auto Click (liên tục)",
    CurrentValue = false,
    Flag = "AutoClick",
    Callback = function(State)
        autoClickEnabled = State
        if State then
            autoClickThread = task.spawn(function()
                while autoClickEnabled do
                    pcall(function()
                        VirtualUser:ClickButton1()
                    end)
                    task.wait(0.05)
                end
            end)
        else
            if autoClickThread then
                task.cancel(autoClickThread)
                autoClickThread = nil
            end
        end
    end
})

-- Aimbot cơ bản (có thể bật cùng với tab Aim)
local aimbotBasicEnabled = false
local aimbotBasicConnection = nil
CombatTab:CreateToggle({
    Name = "Aimbot cơ bản (không smooth)",
    CurrentValue = false,
    Flag = "AimbotBasic",
    Callback = function(State)
        aimbotBasicEnabled = State
        if State then
            if aimbotBasicConnection then aimbotBasicConnection:Disconnect() end
            aimbotBasicConnection = RunService.Heartbeat:Connect(function()
                if not aimbotBasicEnabled then return end
                local root = GetRootPart()
                if not root then return end
                local target = nil
                local minDist = 300
                for _, player in ipairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character then
                        local tr = player.Character:FindFirstChild("HumanoidRootPart")
                        local th = player.Character:FindFirstChild("Humanoid")
                        if tr and th and th.Health > 0 then
                            local dist = (root.Position - tr.Position).Magnitude
                            if dist < minDist then
                                target = tr
                                minDist = dist
                            end
                        end
                    end
                end
                if target then
                    root.CFrame = CFrame.new(root.Position, target.Position)
                end
            end)
        else
            if aimbotBasicConnection then
                aimbotBasicConnection:Disconnect()
                aimbotBasicConnection = nil
            end
        end
    end
})

-- ================================================================
-- TAB AIM (nâng cao: smoothness, tia đỏ, hitbox)
-- ================================================================
local AimSection = AimTab:CreateSection("Cài đặt Aim")

-- Biến điều khiển
local AimSettings = {
    Enabled = false,
    Smoothness = 0.3,
    ShowLaser = false,
    HitboxMultiplier = 1.0,
    TargetPart = "Head"  -- Head, HumanoidRootPart, Torso
}
local CurrentTarget = nil
local LaserPart = nil
local AimConnection = nil
local HitboxConnection = nil
local HitboxTargets = {} -- lưu các part đã mở rộng

-- Toggle bật/tắt Aim
AimTab:CreateToggle({
    Name = "Bật Aim (nâng cao)",
    CurrentValue = false,
    Flag = "AimAdvanced",
    Callback = function(State)
        AimSettings.Enabled = State
        if State then
            if AimConnection then AimConnection:Disconnect() end
            AimConnection = RunService.Heartbeat:Connect(function()
                if not AimSettings.Enabled then return end
                local root = GetRootPart()
                if not root then return end
                -- Tìm mục tiêu gần nhất
                local target = nil
                local minDist = 300
                for _, player in ipairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character then
                        local tr = player.Character:FindFirstChild(AimSettings.TargetPart)
                        if not tr then tr = player.Character:FindFirstChild("HumanoidRootPart") end
                        local th = player.Character:FindFirstChild("Humanoid")
                        if tr and th and th.Health > 0 then
                            local dist = (root.Position - tr.Position).Magnitude
                            if dist < minDist then
                                target = tr
                                minDist = dist
                            end
                        end
                    end
                end
                CurrentTarget = target
                if target then
                    -- Smoothness: nội suy CFrame
                    local targetCF = CFrame.new(root.Position, target.Position)
                    local smooth = AimSettings.Smoothness
                    if smooth >= 1 then
                        root.CFrame = targetCF
                    else
                        local current = root.CFrame
                        local new = current:Lerp(targetCF, smooth)
                        root.CFrame = new
                    end
                end
            end)
            -- Bắt đầu cập nhật laser và hitbox
            UpdateLaserAndHitbox()
        else
            if AimConnection then AimConnection:Disconnect(); AimConnection = nil end
            if LaserPart then LaserPart:Destroy(); LaserPart = nil end
            -- Reset hitbox về kích thước cũ
            ResetAllHitboxes()
            if HitboxConnection then HitboxConnection:Disconnect(); HitboxConnection = nil end
        end
    end
})

-- Slider Smoothness
AimTab:CreateSlider({
    Name = "Độ mượt (0.1 - 1.0)",
    Range = {0.1, 1.0},
    Increment = 0.05,
    Suffix = "smooth",
    CurrentValue = 0.3,
    Flag = "AimSmoothness",
    Callback = function(Value)
        AimSettings.Smoothness = Value
    end
})

-- Dropdown chọn bộ phận nhắm
AimTab:CreateDropdown({
    Name = "Bộ phận nhắm",
    Options = {"Head", "HumanoidRootPart", "Torso"},
    CurrentOption = "Head",
    Flag = "AimTargetPart",
    Callback = function(Option)
        AimSettings.TargetPart = Option
    end
})

-- Slider Hitbox Multiplier
AimTab:CreateSlider({
    Name = "Hệ số hitbox (1 - 5)",
    Range = {1, 5},
    Increment = 0.1,
    Suffix = "x",
    CurrentValue = 1.0,
    Flag = "HitboxMultiplier",
    Callback = function(Value)
        AimSettings.HitboxMultiplier = Value
        -- Cập nhật hitbox ngay nếu đang bật
        if AimSettings.Enabled then
            UpdateHitboxes()
        end
    end
})

-- Toggle Laser (tia đỏ)
AimTab:CreateToggle({
    Name = "Hiển thị tia đỏ (Laser)",
    CurrentValue = false,
    Flag = "ShowLaser",
    Callback = function(State)
        AimSettings.ShowLaser = State
        if State and AimSettings.Enabled then
            CreateLaser()
        else
            if LaserPart then LaserPart:Destroy(); LaserPart = nil end
        end
    end
})

-- Hàm tạo Laser
function CreateLaser()
    if LaserPart then LaserPart:Destroy() end
    LaserPart = Instance.new("Part")
    LaserPart.Name = "AimLaser"
    LaserPart.BrickColor = BrickColor.new("Bright red")
    LaserPart.Material = Enum.Material.Neon
    LaserPart.Anchored = true
    LaserPart.CanCollide = false
    LaserPart.Transparency = 0.2
    LaserPart.Size = Vector3.new(0.2, 0.2, 1)
    LaserPart.Parent = Workspace
    -- Cập nhật laser mỗi frame
    local laserConnection = RunService.Heartbeat:Connect(function()
        if not AimSettings.Enabled or not AimSettings.ShowLaser then
            if LaserPart then LaserPart:Destroy(); LaserPart = nil end
            laserConnection:Disconnect()
            return
        end
        local root = GetRootPart()
        local target = CurrentTarget
        if root and target then
            local startPos = root.Position
            local endPos = target.Position
            local mid = (startPos + endPos) / 2
            local distance = (startPos - endPos).Magnitude
            LaserPart.Size = Vector3.new(0.2, 0.2, distance)
            LaserPart.CFrame = CFrame.new(mid, endPos) * CFrame.Angles(math.rad(90), 0, 0)
            LaserPart.Transparency = 0.2
        else
            if LaserPart then LaserPart.Transparency = 1 end
        end
    end)
end

-- Hàm cập nhật hitbox
function UpdateHitboxes()
    -- Reset hitboxes cũ
    ResetAllHitboxes()
    -- Áp dụng multiplier cho tất cả kẻ địch
    local mult = AimSettings.HitboxMultiplier
    if mult <= 1 then return end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local char = player.Character
            for _, part in ipairs(char:GetChildren()) do
                if part:IsA("BasePart") then
                    local originalSize = part:GetAttribute("OriginalSize")
                    if not originalSize then
                        part:SetAttribute("OriginalSize", part.Size)
                    else
                        part.Size = originalSize * mult
                    end
                    table.insert(HitboxTargets, part)
                end
            end
        end
    end
end

function ResetAllHitboxes()
    for _, part in ipairs(HitboxTargets) do
        if part and part.Parent then
            local orig = part:GetAttribute("OriginalSize")
            if orig then
                part.Size = orig
            end
        end
    end
    HitboxTargets = {}
end

-- Hàm kết hợp laser + hitbox
function UpdateLaserAndHitbox()
    if HitboxConnection then HitboxConnection:Disconnect() end
    HitboxConnection = RunService.Heartbeat:Connect(function()
        if AimSettings.Enabled then
            UpdateHitboxes()
            if AimSettings.ShowLaser then
                if not LaserPart then CreateLaser() end
            else
                if LaserPart then LaserPart:Destroy(); LaserPart = nil end
            end
        else
            ResetAllHitboxes()
            if LaserPart then LaserPart:Destroy(); LaserPart = nil end
        end
    end)
end

-- ================================================================
-- TAB DỊCH CHUYỂN
-- ================================================================
local TeleportSection = TeleportTab:CreateSection("Teleport")

TeleportTab:CreateTextBox({
    Name = "Nhập tọa độ (x,y,z)",
    PlaceholderText = "0,50,0",
    RemoveTextAfterFocusLost = false,
    Flag = "CoordInput",
    Callback = function(Text)
        local parts = {}
        for num in string.gmatch(Text, "[^,]+") do
            table.insert(parts, tonumber(num))
        end
        if #parts == 3 then
            local root = GetRootPart()
            if root then
                root.CFrame = CFrame.new(parts[1], parts[2], parts[3])
            end
        end
    end
})

TeleportTab:CreateButton({
    Name = "Teleport về Spawn",
    Callback = function()
        local spawn = Workspace:FindFirstChild("SpawnLocation")
        if spawn then
            local root = GetRootPart()
            if root then
                root.CFrame = spawn.CFrame + Vector3.new(0, 5, 0)
            end
        else
            Rayfield:Notify({
                Title = "Lỗi",
                Content = "Không tìm thấy SpawnLocation",
                Duration = 3
            })
        end
    end
})

TeleportTab:CreateButton({
    Name = "Teleport lên cao 1000",
    Callback = function()
        local root = GetRootPart()
        if root then
            root.CFrame = root.CFrame + Vector3.new(0, 1000, 0)
        end
    end
})

-- ================================================================
-- TAB ESP
-- ================================================================
local espEnabled = false
local espBillboards = {}
local espConnection = nil

local function CreateESP(player)
    if player == LocalPlayer then return end
    if not player.Character then return end
    local root = player.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    local bill = Instance.new("BillboardGui")
    bill.Name = "ESP_BILL"
    bill.Size = UDim2.new(0, 200, 0, 40)
    bill.Adornee = root
    bill.AlwaysOnTop = true
    bill.Parent = root
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1,0,1,0)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.fromRGB(255,50,50)
    label.TextScaled = true
    label.Font = Enum.Font.SourceSansBold
    label.Text = player.Name
    label.Parent = bill
    espBillboards[player] = bill
end

local function RemoveESP(player)
    if espBillboards[player] then
        espBillboards[player]:Destroy()
        espBillboards[player] = nil
    end
end

local function UpdateESP()
    for _, player in ipairs(Players:GetPlayers()) do
        if espEnabled then
            if player ~= LocalPlayer and player.Character then
                if not espBillboards[player] then CreateESP(player) end
                local root = GetRootPart()
                local targetRoot = player.Character:FindFirstChild("HumanoidRootPart")
                if espBillboards[player] and root and targetRoot then
                    local label = espBillboards[player]:FindFirstChildOfClass("TextLabel")
                    if label then
                        local dist = math.floor((root.Position - targetRoot.Position).Magnitude)
                        label.Text = player.Name .. " | " .. dist .. "m"
                    end
                end
            end
        else
            RemoveESP(player)
        end
    end
end

ESPTab:CreateToggle({
    Name = "Bật/Tắt ESP (tên + khoảng cách)",
    CurrentValue = false,
    Flag = "ESP",
    Callback = function(State)
        espEnabled = State
        if State then
            if espConnection then espConnection:Disconnect() end
            espConnection = RunService.Heartbeat:Connect(UpdateESP)
            UpdateESP()
        else
            if espConnection then espConnection:Disconnect(); espConnection = nil end
            for player, _ in pairs(espBillboards) do RemoveESP(player) end
        end
    end
})
Players.PlayerRemoving:Connect(function(player) RemoveESP(player) end)

-- ================================================================
-- TAB TÙY CHỈNH
-- ================================================================
local MiscSection = MiscTab:CreateSection("Cài đặt giao diện")

MiscTab:CreateButton({
    Name = "🔁 Reset tất cả cài đặt",
    Callback = function()
        -- Reset các flag về mặc định (thực tế Rayfield tự lưu, ta chỉ thông báo)
        Rayfield:Notify({
            Title = "Đã reset",
            Content = "Khởi động lại script để áp dụng",
            Duration = 3
        })
    end
})

MiscTab:CreateButton({
    Name = "📋 Thông tin",
    Callback = function()
        Rayfield:Notify({
            Title = "Universal Hub",
            Content = "Version 3.0 | Rayfield UI | nhatanh Dev",
            Duration = 5
        })
    end
})

-- ===== THÔNG BÁO KHỞI ĐỘNG =====
Rayfield:Notify({
    Title = "Universal Hub",
    Content = "Đã tải thành công! Nhấn F5 để mở/đóng menu.",
    Duration = 4
})

print("[+] Universal Hub (Rayfield) đã tải. F5 để mở menu.")
